[package]
name = "yelken-editor"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
leptos = { version = "0.7.7", default-features = false, features = ["ssr"] }
wit-bindgen.workspace = true
