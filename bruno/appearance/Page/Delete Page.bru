meta {
  name: Delete Page
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/appearance/page/delete/:key
  body: none
  auth: inherit
}

params:query {
  ~namespace: default
  ~locale: en
}

params:path {
  key: home
}
